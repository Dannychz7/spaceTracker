@page "/astronauts"
@using spaceTracker.Models
@using spaceTracker.Services
@inject SpaceDevsService spaceDevsService

<PageTitle>Astronauts</PageTitle>

<h1>Famous Astronauts</h1>

<!-- Optional search bar -->
<input type="text" placeholder="Search astronaut..." @bind="searchTerm" />

<!-- Container for animated card/overlay pages -->
<div class="astronaut-stack">
    @* TODO: Loop through astronauts from spaceDevsService *@
    @* Each astronaut will be a "page" or "card" in the stack *@
    @* Animate pages sliding left/right or stacking on top of each other *@
    @* Include astronaut details: name, DOB, description, flights, etc. *@
    @* Include navigation buttons for next/previous *@
    @* Optional: swipe gestures for mobile *@
</div>

@code {
    private string searchTerm = string.Empty;

    // TODO: Create a variable to hold current page index for animation
    // private int currentIndex = 0;

    // TODO: Methods to show next/previous astronaut card
    // private void ShowNext() { ... }
    // private void ShowPrevious() { ... }

    // TODO: Optional: Method to filter astronauts based on search term
    // private IEnumerable<Astronaut> FilteredAstronauts() { ... }

    // TODO: OnInitializedAsync can optionally fetch astronauts if needed
    // protected override async Task OnInitializedAsync()
    // {
    //     var data = await spaceDevsService.GetAstronautsAsync();
    //     // store data locally for rendering
    // }
}
